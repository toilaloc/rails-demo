<table class="table">
  <thead>
    <tr>
      <th>Title</th>
      <th>Content</th>
    </tr>
  </thead>

  <tbody>
   <% if @posts != nil %>
    <% @posts.each do |post| %>
      <tr>
        <td><%= post.title %></td>
        <td><%= post.content %></td>
        <td><%= link_to "Show", [:admin, post], class: "btn btn-success" %></td>
      <td><%= link_to "Edit", edit_admin_post_path(post), class: "btn btn-primary"%></td>
      <td><%= link_to "Destroy", [:admin, post], method: :delete, data: { confirm: "Are you sure?" }, class: "btn btn-danger" %></td>
      </tr>
    <% else %>
    <tr>
      <td><%= "No post" %></td>
    </tr>
    <% end %>
    <% end %>
  </tbody>
</table>